
import vue from "vue";

import users from "./users/index.js";

const mods = {
	users,
}

//for (let modname in mods) {
	//const mod = mods[modname];
	//for (let stylename in mod) {
		//const comp = mod[stylename];
		//vue.component(`${modname}-${stylename}`, comp);
	//}
//}

export default mods;
